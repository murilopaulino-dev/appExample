import React from 'react';
import { Text, SafeAreaView } from 'react-native';

const Signup = () => {
  return (
    <SafeAreaView>
      <Text>SignUp</Text>
    </SafeAreaView>
  );
};

export default React.memo(Signup);
